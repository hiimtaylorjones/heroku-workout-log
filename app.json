{
  "addons": [
    "heroku-postgresql"
  ],
  "buildpacks": [
    { "url": "heroku/ruby" }
  ],
  "description": "Workout Log API",
  "env": {
    "RACK_ENV": {
      "required": true
    },
    "RAILS_ENV": {
      "required": true
    },
    "SECRET_KEY_BASE": {
      "required": true
    }
  },
  "formation": {
    "web": {
      "quantity": 1
    }
  },
  "name": "heroku-workout-log",
  "stack": "heroku-18",
  "environments": {
    "test": {
      "scripts": {
        "test": "bundle exec rspec spec"
      }
    }
  }
}
