<h3>Iron</h3>

<div class-"workout-list">
    <% if @workouts %>
        <ul>
            <% @workouts.each do |workout| %>
                <li><%= link_to "#{workout.created_at} #{workout.name}", workout_path(workout) %></li>
            <% end %>
        </ul>
    <% else %>
        <h4>No workouts found</h4>
    <% end %>
</div>

<%= link_to "Log Workout", new_workout_path %>

<h3>Soil</h3>

<%= link_to "Log Nutrition", "#" %>